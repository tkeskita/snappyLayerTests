FoamFile
{
    version         2;
    format          ascii;
    class           dictionary;
    object          snappyHexMeshDict;
}

// Exported by SnappyHexMesh GUI add-on for Blender v1.5
// Source file: /path/to/motorBike_003.blend
// Export date: 2024-01-26 11:49:50.320784

maxNonOrtho     35;
maxBoundarySkewness 20;
maxInternalSkewness 4;
maxConcave      80;
minVol          1e-15;
minTetQuality   1e-15;
minArea         1e-30; // -1;
minTwist        0.01; // 0.05;
minDeterminant  0.001;
minFaceWeight   0.01; // 0.05;
minVolRatio     0.01;
minTriangleTwist 0.6;

// Relaxed quality criteria for layer addition phase

relaxed {
    maxNonOrtho 70;
    minTriangleTwist 0.5;
    // minTetQuality -1e+30;
}
